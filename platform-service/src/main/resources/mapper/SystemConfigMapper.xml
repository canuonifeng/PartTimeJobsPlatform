<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.platform.mapper.SystemConfigMapper">

    <resultMap id="SystemConfigResultMap" type="com.parttime.platform.pojo.entity.SystemConfig">
        <id property="id" column="id"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="name" column="name"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByKey" resultMap="SystemConfigResultMap">
        SELECT * FROM system_configs WHERE config_key = #{configKey}
    </select>

    <select id="findAll" resultMap="SystemConfigResultMap">
        SELECT * FROM system_configs ORDER BY config_key ASC
    </select>

    <update id="updateByKey">
        UPDATE system_configs SET config_value = #{configValue} WHERE config_key = #{configKey}
    </update>

</mapper>
