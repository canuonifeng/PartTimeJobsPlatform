<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.platform.mapper.EnterpriseRealNameAuthMapper">

    <select id="findPage" resultType="com.parttime.platform.pojo.entity.EnterpriseRealNameAuth">
        SELECT a.id, a.enterprise_id,
               a.legal_person_name, a.legal_person_id_card,
               a.unified_social_credit_code, a.business_license_url,
               a.status, a.reject_reason, a.submitted_at, a.reviewed_at, a.reviewer_id,
               a.created_at, a.updated_at
        FROM enterprise_real_name_auth a
        <where>
            <if test="status != null and status != ''">a.status = #{status}</if>
        </where>
        ORDER BY a.submitted_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM enterprise_real_name_auth a
        <where>
            <if test="status != null and status != ''">a.status = #{status}</if>
        </where>
    </select>

    <select id="findById" resultType="com.parttime.platform.pojo.entity.EnterpriseRealNameAuth">
        SELECT a.id, a.enterprise_id,
               a.legal_person_name, a.legal_person_id_card,
               a.unified_social_credit_code, a.business_license_url,
               a.status, a.reject_reason, a.submitted_at, a.reviewed_at, a.reviewer_id,
               a.created_at, a.updated_at
        FROM enterprise_real_name_auth a
        WHERE a.id = #{id}
    </select>

    <update id="updateReview">
        UPDATE enterprise_real_name_auth
        SET status = #{status},
            reject_reason = #{rejectReason},
            reviewer_id = #{reviewerId},
            reviewed_at = #{reviewedAt}
        WHERE id = #{id}
    </update>

</mapper>
