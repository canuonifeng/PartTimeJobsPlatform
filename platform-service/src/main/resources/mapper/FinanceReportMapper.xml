<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.platform.mapper.FinanceReportMapper">

    <select id="sumApprovedTopUpGroupByDate" resultType="com.parttime.platform.pojo.po.DailyAggRow">
        SELECT DATE(audited_at) AS date, COALESCE(SUM(amount), 0) AS amount
        FROM enterprise_top_up_records
        WHERE status = 'APPROVED'
          AND audited_at &gt;= #{startDate}
          AND audited_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
        GROUP BY DATE(audited_at)
    </select>

    <select id="sumApprovedTopUpBetween" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0) FROM enterprise_top_up_records
        WHERE status = 'APPROVED'
          AND audited_at &gt;= #{startDate}
          AND audited_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
    </select>

    <select id="sumCompletedWithdrawalGroupByDate" resultType="com.parttime.platform.pojo.po.DailyAggRow">
        SELECT DATE(completed_at) AS date, COALESCE(SUM(amount), 0) AS amount
        FROM c_withdrawal_record
        WHERE status = 'COMPLETED'
          AND completed_at &gt;= #{startDate}
          AND completed_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
        GROUP BY DATE(completed_at)
    </select>

    <select id="sumCompletedWithdrawalBetween" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0) FROM c_withdrawal_record
        WHERE status = 'COMPLETED'
          AND completed_at &gt;= #{startDate}
          AND completed_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
    </select>

    <select id="sumCompletedWithdrawalToday" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0) FROM c_withdrawal_record
        WHERE status = 'COMPLETED' AND DATE(completed_at) = CURDATE()
    </select>

</mapper>
