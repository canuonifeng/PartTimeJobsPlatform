<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.platform.mapper.BannerMapper">

    <resultMap id="BannerResultMap" type="com.parttime.platform.pojo.entity.Banner">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="imageUrl" column="image_url"/>
        <result property="linkUrl" column="link_url"/>
        <result property="position" column="position"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="clickCount" column="click_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="BannerResultMap">
        SELECT * FROM banners ORDER BY sort_order ASC, created_at DESC
    </select>

    <select id="findById" resultMap="BannerResultMap">
        SELECT * FROM banners WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banners (title, image_url, link_url, position, sort_order, status, start_time, end_time)
        VALUES (#{title}, #{imageUrl}, #{linkUrl}, #{position}, #{sortOrder}, #{status}, #{startTime}, #{endTime})
    </insert>

    <update id="update">
        UPDATE banners
        SET title = #{title},
            image_url = #{imageUrl},
            link_url = #{linkUrl},
            position = #{position},
            sort_order = #{sortOrder},
            status = #{status},
            start_time = #{startTime},
            end_time = #{endTime}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE banners SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM banners WHERE id = #{id}
    </delete>

</mapper>
