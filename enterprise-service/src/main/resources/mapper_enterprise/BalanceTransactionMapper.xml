<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.BalanceTransactionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO balance_transactions (worker_id, amount, type, related_bill_id, description)
        VALUES (#{workerId}, #{amount}, #{type}, #{relatedBillId}, #{description})
    </insert>

</mapper>
