<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.EnterpriseRealNameAuthMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO enterprise_real_name_auth (
            enterprise_id, legal_person_name, legal_person_id_card,
            unified_social_credit_code, business_license_url,
            status, reject_reason, submitted_at, reviewed_at, reviewer_id
        ) VALUES (
            #{enterpriseId}, #{legalPersonName}, #{legalPersonIdCard},
            #{unifiedSocialCreditCode}, #{businessLicenseUrl},
            #{status}, #{rejectReason}, #{submittedAt}, #{reviewedAt}, #{reviewerId}
        )
    </insert>

    <update id="update">
        UPDATE enterprise_real_name_auth
        SET legal_person_name = #{legalPersonName},
            legal_person_id_card = #{legalPersonIdCard},
            unified_social_credit_code = #{unifiedSocialCreditCode},
            business_license_url = #{businessLicenseUrl},
            status = #{status},
            reject_reason = #{rejectReason},
            submitted_at = #{submittedAt},
            reviewed_at = #{reviewedAt},
            reviewer_id = #{reviewerId}
        WHERE enterprise_id = #{enterpriseId}
    </update>

    <select id="findByEnterpriseId" resultType="com.parttime.enterprise.pojo.entity.EnterpriseRealNameAuth">
        SELECT id, enterprise_id, legal_person_name, legal_person_id_card,
               unified_social_credit_code, business_license_url,
               status, reject_reason, submitted_at, reviewed_at, reviewer_id,
               created_at, updated_at
        FROM enterprise_real_name_auth WHERE enterprise_id = #{enterpriseId}
    </select>

</mapper>
