<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.JobMapper">

    <resultMap id="JobResultMap" type="com.parttime.enterprise.pojo.entity.Job">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="requirements" column="requirements"/>
        <result property="contactName" column="contact_name"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="location" column="location"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="address" column="address"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="categoryId" column="category_id"/>
        <result property="headcount" column="headcount"/>
        <result property="status" column="status"/>
        <result property="deadline" column="deadline"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="imageUrl" column="image_url"/>
        <result property="taskType" column="task_type"/>
        <result property="pricingMode" column="pricing_mode"/>
        <result property="pricePerUnit" column="price_per_unit"/>
        <result property="totalItems" column="total_items"/>
        <result property="externalTaskId" column="external_task_id"/>
        <result property="externalSystemType" column="external_system_type"/>
        <result property="autoSettle" column="auto_settle"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO jobs (company_id, title, description, requirements, contact_name, contact_phone, location, province, city, district, address, latitude, longitude, category_id, headcount, status, deadline, image_url,
                          task_type, pricing_mode, price_per_unit, total_items, external_task_id, external_system_type, auto_settle)
        VALUES (#{companyId}, #{title}, #{description}, #{requirements}, #{contactName}, #{contactPhone}, #{location}, #{province}, #{city}, #{district}, #{address}, #{latitude}, #{longitude}, #{categoryId}, #{headcount}, #{status}, #{deadline}, #{imageUrl},
                #{taskType}, #{pricingMode}, #{pricePerUnit}, #{totalItems}, #{externalTaskId}, #{externalSystemType}, #{autoSettle})
    </insert>

    <select id="findById" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="JobResultMap">
        SELECT * FROM jobs ORDER BY created_at DESC
    </select>

    <select id="findByCompanyId" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE company_id = #{companyId} ORDER BY created_at DESC
    </select>

    <select id="findByCompanyIdAndStatus" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE company_id = #{companyId} AND status = #{status} ORDER BY created_at DESC
    </select>

    <select id="findByCategoryId" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE category_id = #{categoryId} ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE jobs SET status = #{status} WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE jobs
        SET title = #{title}, description = #{description}, requirements = #{requirements}, contact_name = #{contactName}, contact_phone = #{contactPhone}, location = #{location},
            province = #{province}, city = #{city}, district = #{district}, address = #{address},
            latitude = #{latitude}, longitude = #{longitude},
            category_id = #{categoryId}, headcount = #{headcount}, deadline = #{deadline},
            image_url = #{imageUrl},
            task_type = #{taskType}, pricing_mode = #{pricingMode}, price_per_unit = #{pricePerUnit},
            total_items = #{totalItems}, external_task_id = #{externalTaskId}, external_system_type = #{externalSystemType},
            auto_settle = #{autoSettle}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM jobs WHERE id = #{id}
    </delete>

    <select id="findByIds" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByExternalTaskId" resultMap="JobResultMap">
        SELECT * FROM jobs WHERE external_task_id = #{externalTaskId} LIMIT 1
    </select>

</mapper>
