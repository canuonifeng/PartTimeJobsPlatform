<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.EnterpriseAccountMapper">

    <resultMap id="AccountResultMap" type="com.parttime.enterprise.pojo.entity.EnterpriseAccount">
        <id property="id" column="id"/>
        <result property="enterpriseId" column="enterprise_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="displayName" column="display_name"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="AccountResultMap">
        SELECT * FROM enterprise_accounts WHERE username = #{username}
    </select>

    <select id="findByEnterpriseIdPage" resultMap="AccountResultMap">
        SELECT * FROM enterprise_accounts WHERE enterprise_id = #{enterpriseId} ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByEnterpriseId" resultType="long">
        SELECT COUNT(*) FROM enterprise_accounts WHERE enterprise_id = #{enterpriseId}
    </select>

    <select id="findById" resultMap="AccountResultMap">
        SELECT * FROM enterprise_accounts WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO enterprise_accounts (enterprise_id, username, password, display_name, phone, role, status)
        VALUES (#{enterpriseId}, #{username}, #{password}, #{displayName}, #{phone}, #{role}, #{status})
    </insert>

    <update id="update">
        UPDATE enterprise_accounts
        SET display_name = #{displayName}, phone = #{phone}, role = #{role}, status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateProfile">
        UPDATE enterprise_accounts
        SET display_name = #{displayName}, phone = #{phone}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE enterprise_accounts SET password = #{password} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM enterprise_accounts WHERE id = #{id}
    </delete>

</mapper>
