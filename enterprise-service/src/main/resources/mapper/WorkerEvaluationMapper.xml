<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.WorkerEvaluationMapper">

    <resultMap id="EvaluationResultMap" type="com.parttime.enterprise.pojo.entity.WorkerEvaluation">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="jobId" column="job_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="rating" column="rating"/>
        <result property="comment" column="comment"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO worker_evaluations (company_id, job_id, worker_id, rating, comment)
        VALUES (#{companyId}, #{jobId}, #{workerId}, #{rating}, #{comment})
    </insert>

    <select id="findByWorkerIdAndCompanyId" resultMap="EvaluationResultMap">
        SELECT * FROM worker_evaluations
        WHERE worker_id = #{workerId} AND company_id = #{companyId}
        ORDER BY created_at DESC
    </select>

    <select id="findByWorkerId" resultMap="EvaluationResultMap">
        SELECT * FROM worker_evaluations
        WHERE worker_id = #{workerId}
        ORDER BY created_at DESC
    </select>

    <select id="findAvgRatingByWorkerIdAndCompanyId" resultType="java.lang.Double">
        SELECT CAST(AVG(rating) AS DOUBLE) FROM worker_evaluations
        WHERE worker_id = #{workerId} AND company_id = #{companyId}
    </select>

</mapper>
