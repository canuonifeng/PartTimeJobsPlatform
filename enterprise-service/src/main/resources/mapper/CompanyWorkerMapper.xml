<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.CompanyWorkerMapper">

    <resultMap id="CompanyWorkerResultMap" type="com.parttime.enterprise.pojo.entity.CompanyWorker">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="status" column="status"/>
        <result property="firstContactAt" column="first_contact_at"/>
        <result property="lastContactAt" column="last_contact_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="upsert">
        INSERT INTO company_workers (company_id, worker_id, status)
        VALUES (#{companyId}, #{workerId}, 'ACTIVE')
        ON DUPLICATE KEY UPDATE last_contact_at = NOW(), updated_at = NOW()
    </insert>

    <select id="findByCompanyId" resultMap="CompanyWorkerResultMap">
        SELECT cw.*
        FROM company_workers cw
        LEFT JOIN c_worker w ON cw.worker_id = w.id
        WHERE cw.company_id = #{companyId}
        <if test="keyword != null and keyword != ''">
            AND (w.name LIKE CONCAT('%', #{keyword}, '%') OR w.phone LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY cw.last_contact_at DESC
    </select>

    <select id="findById" resultMap="CompanyWorkerResultMap">
        SELECT * FROM company_workers WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE company_workers SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
