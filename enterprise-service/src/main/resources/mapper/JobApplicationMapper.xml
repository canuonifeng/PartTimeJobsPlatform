<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.JobApplicationMapper">

    <resultMap id="JobApplicationResultMap" type="com.parttime.enterprise.pojo.entity.JobApplication">
        <id property="id" column="id"/>
        <result property="scheduleId" column="schedule_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="status" column="status"/>
        <result property="appliedAt" column="applied_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO schedule_applications (schedule_id, worker_id, status)
        VALUES (#{scheduleId}, #{workerId}, #{status})
    </insert>

    <select id="findById" resultMap="JobApplicationResultMap">
        SELECT * FROM schedule_applications WHERE id = #{id}
    </select>

    <select id="findByJobId" resultMap="JobApplicationResultMap">
        SELECT sa.* FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        WHERE js.job_id = #{jobId}
        ORDER BY sa.applied_at DESC
    </select>

    <select id="findByJobIdPaged" resultMap="JobApplicationResultMap">
        SELECT sa.* FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        WHERE js.job_id = #{jobId}
        ORDER BY sa.applied_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByCompanyId" resultMap="JobApplicationResultMap">
        SELECT sa.* FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        JOIN jobs j ON js.job_id = j.id
        WHERE j.company_id = #{companyId}
        ORDER BY sa.applied_at DESC
    </select>

    <select id="findByWorkerId" resultMap="JobApplicationResultMap">
        SELECT * FROM schedule_applications WHERE worker_id = #{workerId} ORDER BY applied_at DESC
    </select>

    <select id="findAll" resultMap="JobApplicationResultMap">
        SELECT * FROM schedule_applications
    </select>

    <select id="countByJobId" resultType="int">
        SELECT COUNT(*) FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        WHERE js.job_id = #{jobId}
    </select>

    <select id="countByScheduleId" resultType="int">
        SELECT COUNT(*) FROM schedule_applications WHERE schedule_id = #{scheduleId}
    </select>

    <select id="findByJobIdAndWorkerId" resultMap="JobApplicationResultMap">
        SELECT sa.* FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        WHERE js.job_id = #{jobId} AND sa.worker_id = #{workerId}
    </select>

    <select id="countByJobIdAndStatus" resultType="int">
        SELECT COUNT(*) FROM schedule_applications sa
        JOIN job_schedules js ON sa.schedule_id = js.id
        WHERE js.job_id = #{jobId} AND sa.status = #{status}
    </select>

    <update id="updateStatus">
        UPDATE schedule_applications SET status = #{status} WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE schedule_applications SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM schedule_applications WHERE id = #{id}
    </delete>

</mapper>
