<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.JobApplicationMapper">

    <resultMap id="JobApplicationResultMap" type="com.parttime.enterprise.pojo.entity.JobApplication">
        <id property="id" column="id"/>
        <result property="jobId" column="job_id"/>
        <result property="companyId" column="company_id"/>
        <result property="workerId" column="worker_id"/>
        <result property="status" column="status"/>
        <result property="appliedAt" column="applied_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO job_applications (job_id, company_id, worker_id, status)
        VALUES (#{jobId}, #{companyId}, #{workerId}, #{status})
    </insert>

    <select id="findById" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications WHERE id = #{id}
    </select>

    <select id="findByJobId" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications WHERE job_id = #{jobId} ORDER BY applied_at DESC
    </select>

    <select id="findByJobIdPaged" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications
        WHERE job_id = #{jobId}
        ORDER BY applied_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByCompanyId" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications WHERE company_id = #{companyId} ORDER BY applied_at DESC
    </select>

    <select id="findByWorkerId" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications WHERE worker_id = #{workerId} ORDER BY applied_at DESC
    </select>

    <select id="findAll" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications
    </select>

    <select id="countByJobId" resultType="int">
        SELECT COUNT(*) FROM job_applications WHERE job_id = #{jobId}
    </select>

    <select id="countByScheduleId" resultType="int">
        SELECT COUNT(*) FROM job_applications WHERE schedule_id = #{scheduleId}
    </select>

    <select id="findByJobIdAndWorkerId" resultMap="JobApplicationResultMap">
        SELECT * FROM job_applications WHERE job_id = #{jobId} AND worker_id = #{workerId}
    </select>

    <select id="countByJobIdAndStatus" resultType="int">
        SELECT COUNT(*) FROM job_applications WHERE job_id = #{jobId} AND status = #{status}
    </select>

    <update id="updateStatus">
        UPDATE job_applications SET status = #{status} WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE job_applications
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM job_applications WHERE id = #{id}
    </delete>

</mapper>
