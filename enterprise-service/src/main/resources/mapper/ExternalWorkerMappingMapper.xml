<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.enterprise.mapper.ExternalWorkerMappingMapper">

    <resultMap id="ExternalWorkerMappingResultMap" type="com.parttime.enterprise.pojo.entity.ExternalWorkerMapping" autoMapping="true">
        <id property="id" column="id"/>
        <result property="workerId" column="worker_id"/>
        <result property="externalWorkerId" column="external_worker_id"/>
        <result property="externalSystemType" column="external_system_type"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO external_worker_mapping (worker_id, external_system_type, external_worker_id, created_at, updated_at)
        VALUES (#{workerId}, #{externalSystemType}, #{externalWorkerId}, NOW(), NOW())
    </insert>

    <select id="selectByExternalId" resultMap="ExternalWorkerMappingResultMap">
        SELECT * FROM external_worker_mapping
        WHERE external_system_type = #{externalSystemType} AND external_worker_id = #{externalWorkerId}
    </select>

    <select id="selectByWorkerId" resultMap="ExternalWorkerMappingResultMap">
        SELECT * FROM external_worker_mapping WHERE worker_id = #{workerId} ORDER BY created_at DESC
    </select>

</mapper>
