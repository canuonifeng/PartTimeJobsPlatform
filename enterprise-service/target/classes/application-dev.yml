server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parttime_enterprise_dev?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
