<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.cservice.mapper.BalanceTransactionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO balance_transactions (worker_id, amount, type, related_bill_id, related_withdrawal_id, description)
        VALUES (#{workerId}, #{amount}, #{type}, #{relatedBillId}, #{relatedWithdrawalId}, #{description})
    </insert>

    <select id="findByWorkerId" resultType="com.parttime.cservice.pojo.entity.BalanceTransaction">
        SELECT * FROM balance_transactions WHERE worker_id = #{workerId} ORDER BY created_at DESC
    </select>

    <select id="findByWorkerIdPage" resultType="com.parttime.cservice.pojo.entity.BalanceTransaction">
        SELECT * FROM balance_transactions WHERE worker_id = #{workerId} ORDER BY created_at DESC LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByWorkerId" resultType="long">
        SELECT COUNT(*) FROM balance_transactions WHERE worker_id = #{workerId}
    </select>

</mapper>
