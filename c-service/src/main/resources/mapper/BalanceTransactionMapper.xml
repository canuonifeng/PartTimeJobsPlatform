<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.cservice.mapper.BalanceTransactionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO balance_transactions (worker_id, amount, type, related_bill_id, related_attendance_record_id, related_withdrawal_id, description)
        VALUES (#{workerId}, #{amount}, #{type}, #{relatedBillId}, #{relatedAttendanceRecordId}, #{relatedWithdrawalId}, #{description})
    </insert>

    <select id="findByWorkerIdPage" resultType="com.parttime.cservice.pojo.entity.BalanceTransaction">
        SELECT bt.*
        FROM balance_transactions bt
        WHERE bt.worker_id = #{workerId}
        ORDER BY bt.created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByWorkerId" resultType="long">
        SELECT COUNT(*) FROM balance_transactions WHERE worker_id = #{workerId}
    </select>

    <select id="sumMonthlyEarnings" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0)
        FROM balance_transactions
        WHERE worker_id = #{workerId}
          AND type = 'EARNINGS'
          AND amount &gt; 0
          AND created_at &gt;= #{startTime}
          AND created_at &lt; #{endTime}
    </select>

    <delete id="deleteByRelatedWithdrawalId">
        DELETE FROM balance_transactions WHERE related_withdrawal_id = #{relatedWithdrawalId}
    </delete>

</mapper>
