<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.cservice.mapper.QuestionBankQuestionMapper">

    <resultMap id="QuestionBankQuestionMap" type="com.parttime.cservice.pojo.entity.QuestionBankQuestion">
        <id property="id" column="id"/>
        <result property="bankId" column="bank_id"/>
        <result property="questionType" column="question_type"/>
        <result property="stem" column="stem"/>
        <result property="optionsJson" column="options_json"/>
        <result property="answer" column="answer"/>
        <result property="analysis" column="analysis"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="countPublishedByBankAndType" resultType="int">
        SELECT COUNT(*) FROM question_bank_questions
        WHERE bank_id = #{bankId} AND question_type = #{questionType} AND status = 'PUBLISHED'
    </select>

    <select id="randomPublishedByBankAndType" resultMap="QuestionBankQuestionMap">
        SELECT * FROM question_bank_questions
        WHERE bank_id = #{bankId} AND question_type = #{questionType} AND status = 'PUBLISHED'
        ORDER BY RAND()
        LIMIT #{count}
    </select>

</mapper>
