<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parttime.cservice.mapper.WithdrawalRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO c_withdrawal_record (worker_id, amount, status, bank_info, remark, processed_at, created_at, updated_at)
        VALUES (#{workerId}, #{amount}, #{status}, #{bankInfo}, #{remark}, #{processedAt}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultType="com.parttime.cservice.pojo.entity.WithdrawalRecord">
        SELECT * FROM c_withdrawal_record WHERE id = #{id}
    </select>

    <select id="findByWorkerId" resultType="com.parttime.cservice.pojo.entity.WithdrawalRecord">
        SELECT * FROM c_withdrawal_record WHERE worker_id = #{workerId}
    </select>

    <update id="updateStatus">
        UPDATE c_withdrawal_record SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
