## ADDED Requirements

### Requirement: 岗位创建后无缝跳转到班次创建
系统须在岗位发布成功后，自动引导用户进入该岗位的班次创建页面，而非返回岗位列表。

#### Scenario: 岗位发布成功后跳转
- **WHEN** 企业用户完成岗位创建并点击"发布"
- **THEN** 系统发布岗位成功后跳转到班次管理页（带该岗位 ID），并在顶部提示"岗位已创建，请添加班次"

#### Scenario: 保存草稿后不跳转
- **WHEN** 企业用户点击"保存草稿"
- **THEN** 系统保存成功后返回岗位列表，不跳转到班次管理页
